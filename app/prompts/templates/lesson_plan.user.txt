请把下面的教案内容转换为学生版“作业任务书”（JSON 输出）。

输入概况：
- 标题：{title}
- 主题：{topic}
- 学段：{school_stage}
- 年级：{grade}
- 作业类型：{assignment_type}
- 探究深度：{inquiry_depth}
- 提交模式：{submission_mode}
- 周期：{duration_weeks} 周
- 主学科：{main_subject}
- 融合学科：{related_subjects}

教案摘录：
{lesson_plan_excerpt}

硬性要求（必须全部满足）：

1) objectives.process 必须两行：
   - 第1行“背景设定：”开头，单段 180-260 字，必须包含：时间线索、具体地点、具体人物/对象、触发事件/冲突、任务目标、限制条件、成功标准。
   - 第2行“行动主线：”开头，40-80 字，总结整条任务链。
2) 背景设定风格：写实校园 + 轻剧情。叙事框架从以下任选 1 个，但不要写出框架名：
   校园新闻调查 / 后勤委托任务书 / 圆桌听证会 / 事故复盘与修复 / 校园挑战赛 / 社团行动计划。
3) 背景设定必须出现 2-4 个“具体名词细节”。
4) phases 只允许 3-4 个阶段，阶段 title 必须像章节标题。
5) 每阶段 1 个核心 step（最多 2 个；第 2 个必须以“（可选）”开头）。
   - step.name：动词短语（6-12字）
   - step.content：情境承接句（50-90字），必须呼应背景设定的具体细节
   - step.description：学习支架（140-240字），先场景钩子，再做法提示
6) checkpoints 每步仅 1-2 条，写清证据、格式与标准，并填对 evidence_type。
7) 保留教案的教学意图与活动顺序；不要随意增加教案没有的“重大结论”。

结构模板（保持兼容；允许合并阶段，但不要新增超过 4 阶段）：
{template_json}

{rag_section}
